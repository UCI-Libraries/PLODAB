
0. directives, restricting to element v attribute:
  http://stackoverflow.com/questions/23220976/angularjs-directive-restrict-a-vs-e

1. ng-transclude:

  ranty blog post: http://teropa.info/blog/2015/06/09/transclusion.html
  simple example: http://jsfiddle.net/7LRDS/1/

2. create a service:
    http://blog.thoughtram.io/angular/2015/07/07/service-vs-factory-once-and-for-all.html

    app.service('myService', function() {

      // service is just a constructor function
      // that will be called with 'new'

      this.sayHello = function(name) {
         return "Hi " + name + "!";
      };
    });

3.   inject a service:
     same as injecting $scope, example:

     angular.module('plodab', [])
       .controller('chartctrl', ['$scope', 'MyService', 'MyOtherService', function ($scope, MyService, MyOtherService) {
         init();

         function init() {
           $scope.topicButtons = [
             {name: "subjects", display: "Subjects"},
             {name: "materials", display: "Materials"},
             {name: "techniques", display: "Techniques"},
             {name: "place_of_publication", display: "Place of Publication"}
           ];

           MyService.sayHello();



4. move chart div that currently has ng-controller into own directive:

   .directive('chartDirective', function() {
    ...
    return {
      controller: 'chartctrl'
      templateUrl: 'chartdirective.html'
      ...
    }
   })

Need a template that is separate html

5. separate files for everything
